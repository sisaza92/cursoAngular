import { NuevoPipePipe } from './nuevo-pipe.pipe';

describe('NuevoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NuevoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
